export enum UniswapEndpoints {
  v1Uniswap = 'checkout/v1/uniswap',
  estimate = 'estimate',
}

export enum BridgeEndpoints {
  v1Bridge = 'checkout/v1/bridge',
  BundlerAddress = 'bundler-address',
}
